import CarScene from "@/components/store/car-scene";

const Store = () => {
    return (
        <section className="w-full h-full">
            <CarScene />
        </section>
    );
};

export default Store;
